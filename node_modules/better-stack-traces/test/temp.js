fs = require("fs")
require("./better-stack-traces").install({
  collapseLibraries: true,
  before: 10,
  // after: 0
});
function foo(cb) {
  cb(123); // this is a really llong line that shoulbe cut off a bit, once we get to the horse
}


function bar(x) {
  1+1 ; fs.readFileSync("../"); throw new Error("x: " + x)
}

require("coffee-script")
require("./temp2")
foo(bar)