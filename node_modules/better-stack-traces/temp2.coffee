fs = require("fs")
require("./better-stack-traces");
foo = (cb) ->
  cb(123) # this is a really llong line that shoulbe cut off a bit, once we get to the horse


bar = (x) ->
  1+1
  fs.readFileSync("../")
  throw new Error("x: " + x)

foo(bar)